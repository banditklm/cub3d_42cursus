#include "cub3d.h"

void    raycast(t_data *data)
{
    double  angle;
    double  segment;
    double  start_angle;
    int     i;

    segment = data->player.fov / data->map_width;
    start_angle = 
    while (i)
}